import { urlChecker } from "../js/urlChecker"

describe("Testing the url submission functionality", () => {
    test("Testing the urlChecker() function", () => {
         
           expect(urlChecker).toBeDefined();
})});